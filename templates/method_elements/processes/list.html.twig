{% extends 'base.html.twig' %}
{% block title %}Process{% endblock %}


{% block body %}
  <div class="mt-3 d-flex flex-row justify-content-center w-100">
    <a href="#" class="btn btn-primary">Create a Process</a>
  </div>

  <div class="card mt-4">
    <div class="card-header">
      All Processes
    </div>
    <div class="card-body">

      <table class="table m-auto">
        <thead class="bg-light">
        <tr>
          <th scope="col">Index</th>
          <th scope="col">Process Name</th>
          <th scope="col">View</th>
          <th scope="col">Update</th>
        </tr>
        </thead>
        <tbody>
        {#{% for graph in bmdGraphs %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ graph.name }}</td>
                <td><a target="_blank" #}{#href="{{ path('show_pattern',{'id':pattern.id}) }}"#}{#>View</a></td>
                <td><a target="_blank" #}{#href="{{ path('update_pattern',{'id':pattern.id}) }}"#}{#>Update</a></td>
            </tr>
        {% endfor %}#}
        </tbody>
      </table>
    </div>
  </div>

{% endblock %}
